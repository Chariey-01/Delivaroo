import BookPage from '../../routes/BookPage';

/** Compatibility route for the parcel-create feature, using the complete booking flow. */
export default function CreateParcel() {
  return <BookPage />;
}
